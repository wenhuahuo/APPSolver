# PyTorch
# Install the correct build for your platform/CUDA first, for example:
#   pip install torch==2.8.0+cu128 --index-url https://download.pytorch.org/whl/cu128
#   pip install torch==2.8.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.8.0

numpy==2.3.2
pandas==3.0.2
scipy==1.17.1
pyyaml==6.0.2
tqdm==4.66.2
timm==1.0.26
einops==0.8.2
kappamodules==0.1.112

# Optional: only needed when using the LLM-based condition encoder
transformers==5.5.4

# Optional: only needed for visualization
matplotlib==3.10.5
